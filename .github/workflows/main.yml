name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  run-tests:
    strategy:
      fail-fast: false
      matrix:
        os:
          - windows-latest
        python-version:
          - "3.12"
    name: Test
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: List root directory contents
        run: dir

      - name: List contents of login_app
        run: dir login_app

      - name: Set PYTHONPATH and run unit tests
        env:
          PYTHONPATH: ${{ github.workspace }}/login_app
        run: |
          echo "PYTHONPATH is set to %PYTHONPATH%"
          pytest tests/unit_tests/test_auth_services.py

      - name: Set PYTHONPATH and run integration tests
        env:
          PYTHONPATH: ${{ github.workspace }}/login_app
        run: |
          echo "PYTHONPATH is set to %PYTHONPATH%"
          pytest tests/integration_tests/test_auth_router.py
